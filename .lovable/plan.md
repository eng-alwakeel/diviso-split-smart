

# خطة: تحسين واجهة قرار النرد في الشات (UI Polish)

## ملخص
تحسين شامل لمظهر وتجربة استخدام ميزة النرد في شات المجموعات لتتوافق مع هوية Diviso البصرية، مع تحسين تسلسل العناصر والتصويت وعرض النتائج.

---

## 1. تحديث بطاقة قرار النرد (DiceDecisionMessage)

**الملف:** `src/components/chat/messages/DiceDecisionMessage.tsx`

### التصميم الجديد:

**الهيدر:**
- اليسار: 🎲 "قرار جماعي"
- اليمين: Badge يوضح نوع النرد:
  - "قرار سريع ⚡" للـ quick
  - "نرد النشاط 🎯" للـ activity
  - "نرد الأكل 🍽️" للـ food

**عرض النتائج (Result Tiles):**
- نتيجة واحدة: Tile كبيرة واحدة في المنتصف
- قرار سريع (dual): Tile تين جنب بعض:
  - Tile 1: "🎯 النشاط" + emoji + label
  - Tile 2: "🍽️ الأكل" + emoji + label
- تنسيق Tile:
  - `rounded-xl` corners
  - حدود خفيفة `border-white/10`
  - خلفية `bg-[#2D302F]`
  - emoji كبير في الأعلى
  - label بولد تحته

**قسم التصويت:**
- استبدال "0/2 صوت مطلوب" بـ "موافقون: X من Y"
- إضافة Progress Bar تحته
- عرض Avatars المصوتين (حد أقصى 3) مع +N للباقي

**الأزرار:**
- Primary: "صوّت 👍" بلون `#C8F169`
  - إذا صوّت: "✓ صوّتت" قابل للتبديل
- Secondary: "إعادة" بلون محايد داكن
  - بعد الاستخدام: "تمت الإعادة" + disabled
- Share: زر icon صغير في أسفل يسار البطاقة

**حالة القبول:**
- نص "✅ تم اعتماد القرار"
- تعطيل أزرار التصويت والإعادة
- زر CTA رئيسي: "➗ ابدأ التقسيم"

**الحالة المعلقة:**
- نص inline: "تم نشر القرار — صوّتوا عليه الحين"
- بدون toast منفصل

---

## 2. تحسين Bottom Sheet اختيار النرد (DiceChatSheet)

**الملف:** `src/components/chat/DiceChatSheet.tsx`

### التصميم الجديد:

**ثلاث بطاقات متساوية:**

| البطاقة | الأيقونة | العنوان | الوصف |
|---------|----------|---------|-------|
| 1 | 🍽️ | نرد الأكل | وش نطبخ/نأكل اليوم؟ |
| 2 | 🎯 | نرد النشاط | وش نسوي؟ |
| 3 | ⚡ | قرار سريع | نشاط + أكل برمية واحدة |

**التنسيق:**
- بطاقات الأكل والنشاط: flat مع حدود `border-white/10`
- بطاقة القرار السريع فقط: gradient خفيف `from-purple-500/20 to-pink-600/20`
- الخلفية: `bg-[#2D302F]`
- المختار: حدود `border-primary`

**زر الرمي:**
- "ارمِ النرد 🎲"
- بلون `#C8F169` (primary)
- ارتفاع كبير h-14

---

## 3. ألوان وتباعد Diviso

**الألوان:**
```css
--background: #1A1C1E
--card-surface: #2D302F
--primary-accent: #C8F169
--border: rgba(255,255,255,0.10)
```

**التباعد:**
- padding داخل البطاقة: `p-5` (كان `p-4`)
- gap بين العناصر: `gap-4`
- تسلسل واضح: Header > Tiles > Voting > Actions

---

## 4. إضافة ترجمات جديدة

**الملفات:**
- `src/i18n/locales/ar/dice.json`
- `src/i18n/locales/en/dice.json`

**المفاتيح الجديدة:**
```json
{
  "chat": {
    "group_decision": "قرار جماعي",
    "voters": "موافقون",
    "of": "من",
    "start_split": "ابدأ التقسيم",
    "decision_accepted": "تم اعتماد القرار",
    "reroll_done": "تمت الإعادة",
    "activity_tile": "النشاط",
    "food_tile": "الأكل"
  },
  "picker": {
    "food": {
      "name": "نرد الأكل",
      "description": "وش نطبخ/نأكل اليوم؟"
    },
    "activity": {
      "name": "نرد النشاط",
      "description": "وش نسوي؟"
    },
    "quick": {
      "name": "قرار سريع",
      "description": "نشاط + أكل برمية واحدة"
    }
  }
}
```

---

## 5. تحسين حالة التحميل

**في DiceDecisionMessage:**
- استخدام shimmer animation
- skeleton للـ tiles

**في نافذة الرمي (إن وجدت):**
- أيقونة نرد متحركة (dice cube visual)
- shimmer effect

---

## الملفات المتأثرة

| الملف | التغيير |
|-------|---------|
| `src/components/chat/messages/DiceDecisionMessage.tsx` | إعادة كتابة شاملة |
| `src/components/chat/DiceChatSheet.tsx` | تحديث تصميم البطاقات |
| `src/i18n/locales/ar/dice.json` | إضافة ترجمات |
| `src/i18n/locales/en/dice.json` | إضافة ترجمات |

---

## التفاصيل التقنية

### هيكل Result Tile:
```tsx
<div className="flex-1 bg-[#2D302F] rounded-xl border border-white/10 p-4 text-center">
  <p className="text-xs text-muted-foreground mb-2">
    {isActivity ? '🎯 النشاط' : '🍽️ الأكل'}
  </p>
  <span className="text-4xl block mb-2">{result.emoji}</span>
  <p className="font-bold text-lg">{result.label}</p>
</div>
```

### Progress Bar للتصويت:
```tsx
<Progress 
  value={(voteCount / threshold) * 100} 
  className="h-1.5 bg-white/10"
/>
```

### زر المشاركة (icon فقط):
```tsx
<Button variant="ghost" size="icon" className="absolute bottom-4 left-4">
  <Share2 className="w-4 h-4" />
</Button>
```

### منطق Reroll المحدود:
- `canReroll` يتحقق من `decision.rerolled_from === null`
- بعد الضغط: الزر يصبح disabled مع نص "تمت الإعادة"

