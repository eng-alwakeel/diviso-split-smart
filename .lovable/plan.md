
# تحسين إدخال رقم الجوال في البروفايل ليتطابق مع صفحات المصادقة

## المشكلة الحالية

حقل رقم الجوال في صفحة البروفايل (الإعدادات) يستخدم `<Input>` عادي بدون اختيار رمز الدولة. هذا يسبب مشاكل:
- المستخدم ممكن يكتب الرقم بصيغ مختلفة (0505..., 966505..., +966505...)
- لا يوجد اختيار دولة مثل صفحة التسجيل والدخول
- لا يتم تنظيف الأصفار الزائدة تلقائياً
- تجربة غير متسقة بين البروفايل وصفحات المصادقة

## الحل

استبدال `<Input>` العادي بمكون `<PhoneInputWithCountry>` الموجود والمستخدم بالفعل في صفحة المصادقة، مع تحديث `PhoneVerificationDialog` لعرض الرقم بشكل أنظف.

---

## التغييرات المطلوبة

### 1. تعديل `src/components/settings/ProfileTab.tsx`

**استيراد المكون الجديد:**
- إضافة `import { PhoneInputWithCountry } from "@/components/ui/phone-input-with-country";`

**استبدال حقل الجوال (الأسطر 445-461):**
- إزالة `<Input>` العادي للجوال
- استبداله بـ `<PhoneInputWithCountry>` مع نفس المنطق:

```tsx
<PhoneInputWithCountry
  value={profile.phone}
  onChange={(newPhone) => {
    setProfile({...profile, phone: newPhone});
    setPhoneVerified(false);
    if (validationErrors.phone) {
      setValidationErrors((prev) => ({...prev, phone: ""}));
    }
  }}
  placeholder="501234567"
  disabled={!isEditMode}
/>
```

- إزالة زر "تأكيد" المنفصل وإعادة هيكلته ليكون تحت حقل الإدخال الجديد بدلاً من بجانبه (لأن `PhoneInputWithCountry` عريض ويحتاج المساحة الكاملة)

### 2. تعديل `src/components/settings/PhoneVerificationDialog.tsx`

تحسين بسيط لعرض الرقم:
- لا يحتاج تغيير كبير لأن الرقم سيأتي بالفعل بصيغة صحيحة (+966XXXXXXXXX) من `PhoneInputWithCountry`
- الـ dialog يعمل بشكل صحيح كما هو

---

## التفاصيل التقنية

### هيكل حقل الجوال الجديد في البروفايل

```text
قبل التعديل:
[حقل إدخال عادي        ] [زر تأكيد]

بعد التعديل:
[🇸🇦 +966 ▼] [حقل الرقم بدون صفر   ]
[زر تأكيد - بعرض كامل (إذا الرقم تغير)]
```

### الملفات المتأثرة

| الملف | نوع التعديل |
|-------|-------------|
| `src/components/settings/ProfileTab.tsx` | استبدال Input بـ PhoneInputWithCountry + إعادة ترتيب زر التأكيد |

### كيف يعمل PhoneInputWithCountry مع البيانات الحالية

- إذا الرقم المحفوظ بصيغة `+966505212146`: المكون يكتشف الدولة تلقائياً ويعرض الرقم بشكل صحيح
- إذا الرقم فارغ: يعرض placeholder والدولة الافتراضية (السعودية)
- عند التعديل: يرسل الرقم بصيغة `+966XXXXXXXXX` مباشرة -- نفس الصيغة المطلوبة للتحقق عبر OTP
