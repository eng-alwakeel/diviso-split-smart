
# خطة: عرض برنامج المستخدمين المؤسسين في المواقع المحددة

## ملخص الوضع الحالي

| الموقع | الحالة | المطلوب |
|--------|--------|---------|
| صفحة التسجيل `/auth` | البانر موجود **قبل** زر Google | نقله ليكون **بعد** زر Google |
| صفحة Login | ✅ لا يعرض البانر | لا تغيير |
| صفحة الترحيب `Welcome.tsx` | ✅ تعرض رقم المستخدم والمزايا | تحسينات طفيفة |
| صفحة البروفايل `ProfileTab.tsx` | ❌ لا تعرض شيء | إضافة قسم كامل |
| صفحات التجارب `DemoExperience` | ❌ لا تعرض رسالة البرنامج | إضافة رسالة قبل CTA |
| صفحة Launch | ❌ لا تعرض رسالة البرنامج | إضافة رسالة قبل زر التسجيل |

---

## التغييرات المطلوبة

### 1. تحديث FoundingProgramBanner.tsx

**التعديل:** إضافة عنصر "استخدام مجاني بحدود محسّنة"

```typescript
// إضافة عنصر جديد للقائمة
<li className="flex items-center gap-2">
  <Check className="h-4 w-4 text-amber-500 shrink-0" />
  <span>{t('founding_program.free_enhanced')}</span>
</li>
```

---

### 2. تحديث Auth.tsx (Signup Mode)

**التعديل:** نقل FoundingProgramBanner ليكون بعد زر Google وقبل الـ Divider

**من (الحالي):**
```
FoundingProgramBanner
Google Button
Divider
باقي خيارات التسجيل
```

**إلى (المطلوب):**
```
Google Button
FoundingProgramBanner
Divider
باقي خيارات التسجيل
```

---

### 3. إضافة قسم Founding User في ProfileTab.tsx

**التعديل:** إضافة قسم جديد يعرض معلومات المستخدم المؤسس

```typescript
// إضافة imports
import { useFoundingUser } from "@/hooks/useFoundingUser";
import { FoundingBadge } from "@/components/ui/founding-badge";
import { Star } from "lucide-react";

// داخل المكون - استخدام hook
const { userNumber, isFoundingUser, lastActiveAt } = useFoundingUser(userId);

// إضافة قسم جديد بعد قسم البروفايل
{isFoundingUser && (
  <Card className="bg-gradient-to-br from-amber-500/10 ...">
    <CardHeader>
      <CardTitle className="flex items-center gap-2">
        <Star className="h-5 w-5 text-amber-500" />
        {t('founding_program.title')}
      </CardTitle>
    </CardHeader>
    <CardContent className="space-y-4">
      {/* رقم المستخدم */}
      <div className="flex items-center justify-between">
        <span>{t('founding_program.you_are_user')}</span>
        <span className="text-2xl font-bold">#{userNumber}</span>
      </div>
      
      {/* الشارة */}
      <FoundingBadge userNumber={userNumber} size="lg" />
      
      {/* النقاط الشهرية */}
      <div className="border-t pt-4">
        <h4 className="text-sm font-medium mb-2">{t('founding_program.monthly_credits_section')}</h4>
        <p className="text-sm text-muted-foreground">{t('founding_program.monthly_credits_desc')}</p>
        <p className="text-xs text-muted-foreground mt-1">{t('founding_program.activity_requirement')}</p>
        {lastActiveAt && (
          <p className="text-xs text-muted-foreground mt-2">
            {t('founding_program.last_activity')}: {formatDate(lastActiveAt)}
          </p>
        )}
      </div>
    </CardContent>
  </Card>
)}
```

---

### 4. تحديث DemoExperience.tsx

**التعديل:** إضافة رسالة برنامج المؤسسين قبل زر التسجيل

```typescript
// استيراد hook
import { useFoundingProgram } from "@/hooks/useFoundingProgram";

// داخل المكون
const { remaining, isClosed } = useFoundingProgram();

// في قسم CTA (بعد isCompleted)
{isCompleted && !isClosed && (
  <div className="bg-gradient-to-br from-amber-500/10 ... rounded-xl p-3 text-center">
    <p className="text-sm font-medium text-amber-600">
      ⭐ احفظ هذه القسمة وانضم لبرنامج المستخدمين المؤسسين
    </p>
    <p className="text-xs text-muted-foreground mt-1">
      متبقي {remaining} من 1000 مقعد
    </p>
  </div>
)}
```

---

### 5. تحديث LaunchPage.tsx

**التعديل:** إضافة رسالة برنامج المؤسسين قبل زر "سجّل حساب جديد"

```typescript
// استيراد
import { useFoundingProgram } from "@/hooks/useFoundingProgram";

// داخل المكون
const { remaining, isClosed } = useFoundingProgram();

// في قسم Bottom Actions - قبل زر التسجيل
{!isClosed && (
  <div className="bg-gradient-to-br from-amber-500/10 ... rounded-xl p-3 text-center">
    <p className="text-sm font-medium text-amber-600">
      ⭐ انضم لبرنامج المستخدمين المؤسسين
    </p>
    <p className="text-xs text-muted-foreground mt-1">
      متبقي {remaining} من 1000 مقعد
    </p>
  </div>
)}
```

---

### 6. تحديث StickySignupBar.tsx

**التعديل:** تحديث الرسالة لتشمل برنامج المؤسسين

```typescript
// استيراد
import { useFoundingProgram } from "@/hooks/useFoundingProgram";

// داخل المكون
const { remaining, isClosed } = useFoundingProgram();

// تحديث النص
<p className="text-sm font-medium text-amber-600">
  {isClosed ? '⚠️ جربت؟ لا تضيّع القسمة' : '⭐ انضم لبرنامج المؤسسين قبل اكتماله'}
</p>
<p className="text-xs text-muted-foreground mt-1">
  {isClosed 
    ? 'سجّل مجانًا واحصل على 50 نقطة 🎁'
    : `متبقي ${remaining} من 1000 مقعد`
  }
</p>
```

---

### 7. تحديث الترجمات

**ملف:** `src/i18n/locales/ar/auth.json`

```json
{
  "founding_program": {
    // إضافات
    "free_enhanced": "استخدام مجاني بحدود محسّنة",
    "join_founding": "انضم لبرنامج المستخدمين المؤسسين",
    "save_and_join": "احفظ هذه القسمة وانضم لبرنامج المؤسسين",
    "spots_remaining": "متبقي {{remaining}} من {{limit}} مقعد"
  }
}
```

**ملف:** `src/i18n/locales/en/auth.json`

```json
{
  "founding_program": {
    // إضافات
    "free_enhanced": "Free usage with enhanced limits",
    "join_founding": "Join the Founding Users Program",
    "save_and_join": "Save this split and join the Founding Program",
    "spots_remaining": "{{remaining}} of {{limit}} spots remaining"
  }
}
```

---

## ملخص الملفات المعدلة

| الملف | التعديل |
|-------|---------|
| `src/components/auth/FoundingProgramBanner.tsx` | إضافة عنصر "استخدام مجاني" |
| `src/pages/Auth.tsx` | نقل البانر بعد زر Google |
| `src/components/settings/ProfileTab.tsx` | إضافة قسم Founding User كامل |
| `src/components/launch/DemoExperience.tsx` | إضافة رسالة المؤسسين قبل CTA |
| `src/pages/LaunchPage.tsx` | إضافة رسالة المؤسسين قبل زر التسجيل |
| `src/components/launch/StickySignupBar.tsx` | تحديث الرسالة لتشمل البرنامج |
| `src/i18n/locales/ar/auth.json` | إضافة ترجمات جديدة |
| `src/i18n/locales/en/auth.json` | إضافة ترجمات جديدة |

---

## الهيكل النهائي

### صفحة التسجيل `/auth`

```text
┌─────────────────────────────────────────────┐
│ [Logo]                                       │
│ قسّم بذكاء، سافر براحة                      │
│                                             │
│ إنشاء حساب                                  │
├─────────────────────────────────────────────┤
│ [🟢 سجّل بحساب Google - الأسرع]             │  ← Primary CTA
├─────────────────────────────────────────────┤
│ ⭐ برنامج المستخدمين المؤسسين              │  ← بعد زر Google
│ • 100 نقطة ترحيبية                         │
│ • 50 نقطة شهريًا (بدخول واحد شهريًا)       │
│ • رقم مستخدم تسلسلي دائم                   │
│ • استخدام مجاني بحدود محسّنة               │
│ ⏳ متبقي X من 1000                         │
│ *تسري الشروط والأحكام*                     │
├─────────────────────────────────────────────┤
│           ─── أو ───                        │
│ [ باقي خيارات التسجيل ↓ ]                  │
├─────────────────────────────────────────────┤
│ 👥 انضم لمستخدمين...                       │
│ لديك حساب؟ تسجيل الدخول                   │
└─────────────────────────────────────────────┘
```

### صفحة Login (بدون برنامج المؤسسين)

```text
┌─────────────────────────────────────────────┐
│ تسجيل الدخول                               │
├─────────────────────────────────────────────┤
│ الدخول مجاني...                            │  ← Reassurance فقط
├─────────────────────────────────────────────┤
│ [🟢 سجّل بحساب Google - الأسرع]             │
│           ─── أو ───                        │
│ [ باقي خيارات الدخول ↓ ]                   │
├─────────────────────────────────────────────┤
│ [🟣 إنشاء حساب مجاني + 50 نقطة 🎁]         │
└─────────────────────────────────────────────┘
```

### صفحة البروفايل (للمستخدمين المؤسسين)

```text
┌─────────────────────────────────────────────┐
│ [Avatar] اسم المستخدم                       │
│ [⭐ #XXX مستخدم مؤسس]                       │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ ⭐ برنامج المستخدمين المؤسسين              │
├─────────────────────────────────────────────┤
│ أنت المستخدم رقم: #XXX                      │
│ [شارة FoundingBadge]                        │
├─────────────────────────────────────────────┤
│ 💰 النقاط الشهرية                          │
│ تحصل على 50 نقطة شهريًا                    │
│ شرط: تسجيل دخول واحد على الأقل شهريًا       │
│ آخر نشاط: 2026-01-15                       │
└─────────────────────────────────────────────┘
```

### صفحة التجربة / Launch

```text
[بعد اكتمال التجربة]
┌─────────────────────────────────────────────┐
│ ⭐ احفظ هذه القسمة وانضم لبرنامج           │
│    المستخدمين المؤسسين                     │
│ متبقي X من 1000 مقعد                       │
├─────────────────────────────────────────────┤
│ [🟢 ابدأ مجموعتك الحين]                     │
└─────────────────────────────────────────────┘
```

---

## معايير القبول

| # | المعيار |
|---|---------|
| 1 | بانر البرنامج يظهر **بعد** زر Google في صفحة التسجيل |
| 2 | بانر البرنامج **لا يظهر** في صفحة Login |
| 3 | صفحة البروفايل تعرض رقم المستخدم والشارة والنقاط الشهرية |
| 4 | صفحة التجارب تعرض رسالة البرنامج قبل زر التسجيل |
| 5 | صفحة Launch تعرض رسالة البرنامج قبل زر التسجيل |
| 6 | جميع الرسائل تختفي تلقائياً عند اكتمال 1000 مستخدم |
| 7 | الترجمات كاملة للعربية والإنجليزية |

---

## ملخص التغييرات

| النوع | العدد |
|-------|-------|
| ملفات معدلة | 8 |
| مكونات جديدة | 0 |
| ترجمات جديدة | 4 مفاتيح |

**الوقت المتوقع للتنفيذ:** 20-25 دقيقة
