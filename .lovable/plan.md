

# خطة تحسين وإصلاح عرض الاشتراك

## المشاكل المكتشفة من الصورة

| المشكلة | السبب |
|---------|-------|
| `subscription.current_plan` يظهر كنص | قسم `subscription` غير موجود في `ar/settings.json` |
| `subscription.status` يظهر كنص | نفس السبب - ترجمة مفقودة |
| البادج يظهر "Free" | الهوك لا يقرأ الاشتراك بشكل صحيح |
| نوع الخطة "مجاني" | بالرغم من أن الاشتراك `personal + active` في القاعدة |

## بيانات الاشتراك الحالية في قاعدة البيانات

```
plan: personal
status: active
started_at: 2026-01-28
expires_at: 2026-02-28
billing_cycle: monthly
```

---

## التعديلات المطلوبة

### 1. إضافة قسم الترجمات للاشتراك في `ar/settings.json`

إضافة قسم `subscription` كامل يحتوي على:
- `current_plan`: "الخطة الحالية"
- `current_plan_desc`: "تفاصيل اشتراكك الحالي"
- `status`: "حالة الاشتراك"
- `plan_type`: "نوع الخطة"
- `start_date`: "تاريخ البداية"
- `end_date`: "تاريخ الانتهاء"
- `days_remaining`: "المدة المتبقية"
- `subscribe_now`: "اشترك الآن"
- `manage_subscription`: "إدارة الاشتراك"
- `expiry_warning`: "تحذير انتهاء الاشتراك"

### 2. إصلاح الهوك `usePlanBadge.ts`

التحقق من أن الهوك يقرأ الاشتراك بشكل صحيح:
- إذا كان `status === 'active'` و `plan === 'personal'` → يجب أن يعرض `Starter`
- التأكد من أن التحويل `personal → starter` يعمل بشكل صحيح

### 3. تحسين مكون `SubscriptionSettingsTab.tsx`

- تحديث مسارات الترجمة لتستخدم القسم الصحيح
- إضافة fallback للترجمات المفقودة
- تحسين تصميم البطاقة وعرض المعلومات

### 4. إضافة ترجمات للخطط في `ar/settings.json`

```json
"plans": {
  "free": "مجاني",
  "starter": "Starter",
  "pro": "Pro",
  "max": "Max"
}
```

---

## الملفات التي سيتم تعديلها

| الملف | التعديل |
|-------|---------|
| `src/i18n/locales/ar/settings.json` | إضافة قسم `subscription` كامل |
| `src/i18n/locales/en/settings.json` | تحديث قسم `subscription` |
| `src/components/settings/SubscriptionSettingsTab.tsx` | إصلاح مسارات الترجمة وتحسين العرض |
| `src/hooks/usePlanBadge.ts` | التأكد من صحة قراءة الاشتراك |

---

## النتيجة المتوقعة بعد التنفيذ

```text
┌─────────────────────────────────────────────────┐
│  👑 الخطة الحالية                    [⚡ Starter] │
│  تفاصيل اشتراكك الحالي                          │
├─────────────────────────────────────────────────┤
│  📋 نوع الخطة        │  ⏰ حالة الاشتراك        │
│  Starter             │  [نشط ✓]                │
├─────────────────────────────────────────────────┤
│  📅 تاريخ البداية    │  📅 تاريخ الانتهاء      │
│  28 يناير 2026       │  28 فبراير 2026         │
├─────────────────────────────────────────────────┤
│  المدة المتبقية: 31 يوم                         │
│  ═══════════════════░░░░░░░░ (10%)              │
├─────────────────────────────────────────────────┤
│           [إدارة الاشتراك]                      │
└─────────────────────────────────────────────────┘
```

- العناوين ستظهر بالعربي بدل مفاتيح الترجمة
- البادج سيظهر "Starter" بدل "Free"
- حالة الاشتراك ستظهر "نشط" بدل "لا يوجد اشتراك"
- التواريخ والمدة المتبقية ستظهر بشكل صحيح

