

# خطة: إضافة تجربتين داخل صفحات التجارب /launch?demo=…

## الهدف

إضافة خيارين داخل صفحات التجارب:
1. **⚡ تجربة سريعة** (Quick Demo) - الافتراضي
2. **🧩 جرّبها بطريقتك** (Guided Full Demo) - تجربة شبه كاملة

---

## نطاق التعديل

```text
✅ يُطبق على: /launch?demo=travel, /launch?demo=friends, ...
❌ لا يُطبق على: /launch (الصفحة الرئيسية)
```

---

## الملفات المطلوب إنشاءها/تعديلها

| الملف | الوصف | النوع |
|-------|-------|-------|
| `src/components/launch/DemoModeToggle.tsx` | مكون التبديل بين التجربتين | جديد |
| `src/components/launch/QuickDemoView.tsx` | التجربة السريعة (الحالية) | جديد |
| `src/components/launch/FullDemoView.tsx` | التجربة الشبه كاملة | جديد |
| `src/components/launch/FullDemoExpenseForm.tsx` | نموذج إضافة مصروف | جديد |
| `src/components/launch/DemoNavigation.tsx` | أزرار التنقل (رجوع للتجارب / المميزات) | جديد |
| `src/components/launch/DemoExperience.tsx` | تكامل المكونات الجديدة | تعديل |
| `src/data/demoScenarios.ts` | إضافة أنواع للـ Full Demo | تعديل |

---

## 1. DemoModeToggle.tsx - مكون التبديل

```typescript
interface DemoModeToggleProps {
  mode: 'quick' | 'full';
  onModeChange: (mode: 'quick' | 'full') => void;
}
```

### UI

```text
┌───────────────────────────────────────────────────────┐
│  [ ⚡ تجربة سريعة ]     [ 🧩 جرّبها بطريقتك ]       │
│       ↑ active                                        │
└───────────────────────────────────────────────────────┘
```

### السلوك
- الافتراضي: `quick`
- التبديل فوري بدون Reload
- Analytics event: `demo_mode_selected`

---

## 2. QuickDemoView.tsx - التجربة السريعة

نقل المحتوى الحالي من `DemoExperience.tsx` إلى مكون منفصل:
- الأعضاء الجاهزين
- المصاريف الجاهزة
- التفاعل: تغيير الدافع / المبلغ
- عرض الأرصدة

---

## 3. FullDemoView.tsx - التجربة الشبه كاملة

### State المحلي (Local Only)

```typescript
interface FullDemoState {
  members: { id: string; name: string }[];       // 2-5 أعضاء
  expenses: FullDemoExpense[];                   // 1-5 مصاريف
  currentStep: 'members' | 'expenses' | 'results';
}

interface FullDemoExpense {
  id: string;
  description: string;
  amount: number;
  paidById: string;
  splitType: 'equal' | 'percentage' | 'custom';
  splits?: { memberId: string; value: number }[];
}
```

### الخطوات

```text
الخطوة 1: إضافة الأعضاء (2-5)
┌─────────────────────────────────────────┐
│ 👥 من معك في المجموعة؟                 │
│                                          │
│ [إدخال اسم] [+]                         │
│                                          │
│ • أحمد  [×]                              │
│ • سعود  [×]                              │
│                                          │
│ (2/5 أعضاء - الحد الأدنى 2)             │
│                                          │
│        [ التالي → ]                      │
└─────────────────────────────────────────┘

الخطوة 2: إضافة المصاريف (1-5)
┌─────────────────────────────────────────┐
│ 📝 المصاريف                             │
│                                          │
│ ┌─────────────────────────────────────┐ │
│ │ الوصف: [             ]             │ │
│ │ المبلغ: [        ] ر.س             │ │
│ │ دفعها: [أحمد ▾]                    │ │
│ │ القسمة: [بالتساوي ▾]              │ │
│ │                                     │ │
│ │ [ إضافة المصروف ]                  │ │
│ └─────────────────────────────────────┘ │
│                                          │
│ المصاريف المضافة:                        │
│ • عشاء - 450 ر.س (أحمد)                 │
│                                          │
│ (1/5 مصاريف)                            │
│                                          │
│   [ ← السابق ]    [ شوف القسمة → ]      │
└─────────────────────────────────────────┘

الخطوة 3: النتائج + CTA
┌─────────────────────────────────────────┐
│ 💰 القسمة النهائية                      │
│                                          │
│ [DemoBalanceView]                        │
│                                          │
│ ─────────────────────────────────────── │
│                                          │
│ 💾 تبغى تحفظ القسمة وتكمل عليها؟        │
│                                          │
│    [ ابدأ مجموعتك الآن ]                │
│                                          │
│    🔗 شارك التجربة                       │
└─────────────────────────────────────────┘
```

---

## 4. FullDemoExpenseForm.tsx - نموذج المصروف

### Props

```typescript
interface FullDemoExpenseFormProps {
  members: { id: string; name: string }[];
  onAddExpense: (expense: FullDemoExpense) => void;
  expenseCount: number;
  maxExpenses: number;
}
```

### خيارات القسمة

| النوع | الوصف |
|-------|-------|
| `equal` | بالتساوي (افتراضي) |
| `percentage` | بالنِسب |
| `custom` | حسب المشاركين (اختيار من يشارك) |

---

## 5. DemoNavigation.tsx - أزرار التنقل

```typescript
interface DemoNavigationProps {
  onBackToLaunch: () => void;
  onViewFeatures: () => void;
}
```

### UI (أسفل Header)

```text
┌─────────────────────────────────────────┐
│ ← كل التجارب    |    شوف مميزات التطبيق │
└─────────────────────────────────────────┘
```

---

## 6. تعديل DemoExperience.tsx

### التغييرات الرئيسية

```typescript
// إضافة state جديد
const [demoMode, setDemoMode] = useState<'quick' | 'full'>('quick');

// في الـ return
return (
  <div className="fixed inset-0 z-50 bg-background overflow-y-auto" dir="rtl">
    {/* Header */}
    <div className="sticky top-0 ...">
      <button onClick={onClose}>×</button>
      <h1>{scenario.icon} {scenario.groupName}</h1>
    </div>
    
    {/* Navigation Links */}
    <DemoNavigation 
      onBackToLaunch={() => navigate('/launch')}
      onViewFeatures={() => navigate('/')}
    />
    
    {/* Mode Toggle */}
    <DemoModeToggle 
      mode={demoMode} 
      onModeChange={(mode) => {
        setDemoMode(mode);
        trackEvent('demo_mode_selected', { mode });
      }} 
    />
    
    {/* Content based on mode */}
    {demoMode === 'quick' ? (
      <QuickDemoView 
        scenario={scenario}
        onCompleted={onCompleted}
      />
    ) : (
      <FullDemoView 
        scenarioId={scenario.id}
        currency={scenario.currency}
        onCompleted={handleFullDemoCompleted}
      />
    )}
    
    {/* CTA Section after completion */}
    {isCompleted && (
      <CTASection 
        text="💾 تبغى تحفظ القسمة وتكمل عليها؟"
        buttonText="ابدأ مجموعتك الآن"
        onSignup={onSignup}
      />
    )}
  </div>
);
```

---

## 7. Analytics Events

| Event | Parameters | متى |
|-------|------------|-----|
| `demo_mode_selected` | `{ mode: 'quick' \| 'full' }` | عند التبديل |
| `full_demo_step_completed` | `{ step: 'members' \| 'expenses' }` | عند الانتقال |
| `full_demo_completed` | `{ members_count, expenses_count, duration_seconds }` | عند عرض النتائج |
| `back_to_launch_clicked` | `{ from_mode: 'quick' \| 'full' }` | عند الضغط على الرجوع |

---

## 8. قيود التجربة الشبه كاملة

| القيد | القيمة |
|-------|--------|
| عدد الأعضاء | 2 - 5 |
| عدد المصاريف | 1 - 5 |
| الحفظ في DB | ❌ لا |
| Local state فقط | ✅ نعم |

---

## 9. CTA بعد النتائج

```text
💾 تبغى تحفظ القسمة وتكمل عليها؟
       ↓
[ ابدأ مجموعتك الآن ]
```

### السلوك

```typescript
const handleSignup = async () => {
  const { data: { session } } = await supabase.auth.getSession();
  
  if (session) {
    navigate('/create-group');
  } else {
    navigate('/auth?mode=signup&redirect=/create-group');
  }
};
```

---

## هيكل الملفات النهائي

```text
src/components/launch/
├── DemoExperience.tsx       (معدل)
├── DemoModeToggle.tsx       (جديد)
├── DemoNavigation.tsx       (جديد)
├── QuickDemoView.tsx        (جديد - من الكود الحالي)
├── FullDemoView.tsx         (جديد)
├── FullDemoExpenseForm.tsx  (جديد)
├── DemoBalanceView.tsx      (موجود)
├── ExperienceCard.tsx       (موجود)
└── StickySignupBar.tsx      (موجود)
```

---

## معايير القبول

| # | المعيار | الحالة |
|---|---------|--------|
| 1 | صفحة التجربة تحتوي خيارين (سريعة / شبه كاملة) | ⬜ |
| 2 | الافتراضي تجربة سريعة | ⬜ |
| 3 | التبديل لا يسبب إعادة تحميل | ⬜ |
| 4 | التجربة الشبه كاملة تدعم 2-5 أعضاء | ⬜ |
| 5 | التجربة الشبه كاملة تدعم 1-5 مصاريف | ⬜ |
| 6 | خيارات القسمة: بالتساوي / بالنسب / حسب المشاركين | ⬜ |
| 7 | لا تسجيل قبل النتائج | ⬜ |
| 8 | يوجد زر رجوع لـ /launch | ⬜ |
| 9 | CTA التسجيل يفتح Signup مباشرة | ⬜ |
| 10 | Analytics events تُرسل بشكل صحيح | ⬜ |

---

## ملخص التغييرات

| العنصر | العدد |
|--------|-------|
| ملفات جديدة | 5 |
| ملفات معدلة | 2 |
| مكونات UI جديدة | 5 |
| Analytics events جديدة | 4 |

**الوقت المتوقع للتنفيذ:** 45-60 دقيقة

