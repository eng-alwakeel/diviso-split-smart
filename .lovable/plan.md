
# خطة عرض الرقم التسلسلي للمستخدم

## ملخص المطلوب
1. **نقل قسم الرقم التسلسلي للأعلى في صفحة الإعدادات** (حالياً في الأسفل)
2. **إظهار الرقم التسلسلي لجميع المستخدمين** (وليس فقط المؤسسين)
3. **إضافة عرض الرقم في الداشبورد**

---

## التغييرات المطلوبة

### 1. إنشاء مكون جديد: UserNumberBadge
سيكون مكون عام يعرض الرقم التسلسلي لأي مستخدم (مؤسس أو عادي):

| الحالة | الشكل |
|--------|-------|
| مستخدم مؤسس (1-1000) | شارة ذهبية مع نجمة `⭐ #45` |
| مستخدم عادي (1001+) | شارة بسيطة `#1234` |

**الملف:** `src/components/ui/user-number-badge.tsx`

---

### 2. تعديل صفحة الإعدادات (ProfileTab)
**الملف:** `src/components/settings/ProfileTab.tsx`

**التغييرات:**
- نقل عرض الرقم التسلسلي مباشرة بعد الاسم في قسم البيانات الشخصية (الأعلى)
- إزالة الشرط `isFoundingUser` ليظهر لجميع المستخدمين
- إبقاء قسم "برنامج المؤسسين" الكامل للمؤسسين فقط (مع المزايا والنقاط)

**الموقع الجديد:** بجانب اسم المستخدم مباشرة في أعلى البروفايل

---

### 3. إضافة الرقم في الداشبورد
**الملف:** `src/pages/OptimizedDashboard.tsx`

**التغييرات:**
- إضافة عرض الرقم بجانب رسالة الترحيب "مرحباً بك!"
- استخدام المكون الجديد `UserNumberBadge`
- جلب بيانات المستخدم باستخدام `useFoundingUser` hook

---

### 4. تعديل useFoundingUser hook
**الملف:** `src/hooks/useFoundingUser.ts`

**التغييرات:**
- إرجاع `userNumber` دائماً (وليس فقط للمؤسسين)
- الـ hook يعمل بشكل صحيح حالياً، لكن نحتاج التأكد من إرجاع الرقم لجميع المستخدمين

---

### 5. إضافة ترجمات
**الملفات:** `src/i18n/locales/ar/settings.json` و `src/i18n/locales/en/settings.json`

إضافة نص: "رقمك التسلسلي" / "Your user number"

---

## الشكل النهائي

### في صفحة الإعدادات (الأعلى)
```
+----------------------------------+
|  📷 Avatar                       |
|  اسم المستخدم  [#45] [⚙️ Pro]   |
|  email@example.com               |
+----------------------------------+
```

### في الداشبورد
```
+------------------------------------------+
|  مرحباً بك!                    [#45]     |
|  إدارة ذكية للمصاريف المشتركة            |
+------------------------------------------+
```

---

## الملفات المتأثرة

| الملف | نوع التغيير |
|-------|-------------|
| `src/components/ui/user-number-badge.tsx` | إنشاء جديد |
| `src/components/settings/ProfileTab.tsx` | تعديل موقع العرض |
| `src/pages/OptimizedDashboard.tsx` | إضافة عرض الرقم |
| `src/i18n/locales/ar/settings.json` | إضافة ترجمة |
| `src/i18n/locales/en/settings.json` | إضافة ترجمة |

---

## التفاصيل التقنية

### مكون UserNumberBadge الجديد
```tsx
// شارة للمستخدمين العاديين (رمادية/زرقاء)
// شارة للمؤسسين (ذهبية مع نجمة) - يستخدم FoundingBadge الموجود

interface UserNumberBadgeProps {
  userNumber: number;
  isFoundingUser: boolean;
  size?: "sm" | "md" | "lg";
}
```

### منطق العرض
- إذا كان `userNumber <= 1000` و `isFoundingUser = true`: استخدم `FoundingBadge` (الذهبي)
- إذا كان `userNumber > 1000`: استخدم شارة بسيطة بلون محايد
