

# ازالة تكرار زر "ارم النرد" وتوحيد نقطة الفعل

## المشكلة

زر "ارم النرد" يظهر مرتين في الصفحة:
1. داخل `DailyFocusCard` (وضع Re-engagement) -- سطر 88-95
2. داخل `DailyDiceCard` -- كرت مستقل اسفله

هذا يخالف قاعدة: **كل فعل رئيسي يظهر بزر واحد فقط**.

## الحل

تحويل `DailyFocusCard` في وضع Re-engagement من كرت تنفيذي (بزر نرد) الى كرت توجيهي (نص تحفيزي فقط بدون زر). النرد يبقى حصريا في `DailyDiceCard`.

---

## الملفات المعدلة

### 1. `src/components/dashboard/DailyFocusCard.tsx`

**التعديل:** ازالة زر CTA من وضع Re-engagement

قبل:
```text
// Re-engagement mode
- نص: "طولت الغيبة"
- نص فرعي: "خلّينا نرجعها بخطوة بسيطة"
- زر: "ارم النرد" → navigate('/dice')    ← يُحذف
```

بعد:
```text
// Re-engagement mode
- نص: "طولت الغيبة"
- نص فرعي: "خلّينا نرجعها بخطوة بسيطة"
- نص توجيهي صغير: "شوف اقتراح اليوم 🎲"   ← نص فقط بدون زر
```

التغيير المحدد:
- حذف عنصر `<Button>` بالكامل (سطور 88-95)
- اضافة سطر نص توجيهي خفيف (`text-xs text-muted-foreground`) يشير للنرد بدون ان يكون زرا
- ازالة `Dice5` من الـ imports (لم يعد مستخدما)

### 2. `src/i18n/locales/ar/dashboard.json`

اضافة مفتاح جديد:
```text
"daily_focus.reengagement_hint": "شوف اقتراح اليوم 🎲"
```

### 3. `src/i18n/locales/en/dashboard.json`

اضافة مفتاح جديد:
```text
"daily_focus.reengagement_hint": "Check today's suggestion 🎲"
```

---

## التفاصيل التقنية

### الشكل النهائي لكرت Re-engagement

```text
+------------------------------------------+
|  طولت الغيبة 👀                          |
|  خلّينا نرجعها بخطوة بسيطة              |
|  شوف اقتراح اليوم 🎲  (نص فقط)          |
|  آخر مرة استخدمت النرد 🎲 (اذا وُجد)    |
+------------------------------------------+
```

- لا يوجد زر
- النص التوجيهي يلفت النظر للكرت اللي تحته (DailyDiceCard)
- lastActionHint يظهر كالمعتاد

### القاعدة المطبقة

| الفعل | المكان الوحيد المسموح |
|-------|----------------------|
| ارم النرد | `DailyDiceCard` فقط |
| اضف مصروف | `DailyFocusCard` او `MinimalQuickActions` |
| انشئ مجموعة | `MinimalQuickActions` |
| اكمل مهمة العدة | `DailyFocusCard` (onboarding) |

---

## ما لا يتغير

- `DailyDiceCard` -- لا تعديل (يبقى المكان الوحيد لزر النرد)
- `Dashboard.tsx` -- لا تعديل (الترتيب والشروط صحيحة)
- `useDashboardMode.ts` -- لا تعديل
- `SmartPlanCard` -- لا تعديل
- `MinimalQuickActions` -- لا تعديل

---

## ملخص الملفات

| الملف | التعديل |
|-------|--------|
| `src/components/dashboard/DailyFocusCard.tsx` | حذف زر النرد من re-engagement + اضافة نص توجيهي |
| `src/i18n/locales/ar/dashboard.json` | اضافة مفتاح `reengagement_hint` |
| `src/i18n/locales/en/dashboard.json` | اضافة مفتاح `reengagement_hint` |
