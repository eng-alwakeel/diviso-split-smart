

# خطة: إعادة تصميم تجربة النرد - اجعل النرد هو البطل

## ملخص
إعادة تصميم شاملة لتجربة رمي النرد لتشعر المستخدم بـ "لحظة الرمي الحقيقية" مع إحساس بالعشوائية والترقب، بدلاً من بطاقة نتيجة ثابتة.

---

## 1. فلسفة التصميم الجديدة

### المشكلة الحالية:
- النرد يظهر كبطاقة اقتراح ثابتة
- لا يوجد إحساس بالعشوائية أو الترقب
- النتيجة تظهر منفصلة عن النرد

### القاعدة الجديدة:
- **دائماً** اعرض "لحظة رمي النرد" قبل النتيجة
- النتيجة يجب أن تظهر **من** النرد، ليست بطاقة منفصلة
- التدفق: نرد يدور ← يتوقف ← يتحول إلى النتيجة

---

## 2. إعادة تصميم حالة الرمي (Rolling State)

**الملف:** `src/components/dice/AnimatedDice.tsx`

### التصميم الجديد:
- مكعب نرد كبير في المركز (ليس صحن/plate)
- النرد يهتز ويدور لمدة 1.5-2 ثانية
- النص تحته: "النرد يقرر..." أو "خلّنا نشوف وش يطلع 🎲"
- **لا نعرض** أيقونات النتيجة أثناء الرمي
- إضافة haptic خفيف عند البداية والنهاية

### الأنيميشن الجديدة:
```css
@keyframes dice-shake {
  0%, 100% { transform: rotate(0deg) scale(1); }
  10%, 30%, 50%, 70%, 90% { transform: rotate(-15deg) scale(1.05); }
  20%, 40%, 60%, 80% { transform: rotate(15deg) scale(1.05); }
}

@keyframes dice-flip-reveal {
  0% { transform: rotateY(0deg) scale(1); }
  50% { transform: rotateY(90deg) scale(0.9); }
  100% { transform: rotateY(180deg) scale(1); }
}
```

---

## 3. أنيميشن كشف النتيجة (Result Reveal)

**الملف:** `src/components/dice/AnimatedDice.tsx` + `src/components/dice/DiceDecision.tsx`

### التسلسل الجديد:
1. النرد **يتوقف** عن الدوران
2. النرد **يقلب/يتحول** إلى وجه النتيجة
3. النتيجة تظهر **على سطح النرد** أولاً:
   - الإيموجي الكبير
   - التسمية تحته مباشرة
4. **بعدها فقط** → تتوسع إلى البطاقة الكاملة

### مثال (جلسة بيت):
```
[🏠]  ← النرد يظهر الوجه
"جلسة بيت"
    ↓
[بطاقة النتيجة الكاملة تظهر]
```

---

## 4. إعادة تصميم بطاقة النتيجة

**الملف:** `src/components/dice/DiceResult.tsx`

### الهيدر:
- 🎲 قرار جماعي
- Subtitle: "النرد اختار التالي"

### منطقة النتيجة:
- **نتيجة واحدة:** Tile كبيرة واحدة في المركز
  - إيموجي كبير
  - عنوان بولد
  - بدون أزرار داخل الـ Tile

- **قرار سريع (Dual):** Tile تين **فوق بعض** (عمودياً):
  ```
  ┌─────────────────┐
  │ 🎯 النشاط       │
  │    🏠           │
  │  جلسة بيت      │
  └─────────────────┘
  ┌─────────────────┐
  │ 🍽️ الأكل        │
  │    🍚           │
  │  أكلة رز       │
  └─────────────────┘
  ```
  هذا يتجنب الفوضى البصرية ويحافظ على التركيز.

---

## 5. قسم التصويت (منفصل بوضوح)

**الملف:** `src/components/dice/DiceResult.tsx`

### التصميم:
- فاصل (Divider) واضح بعد الـ Tiles
- نص السؤال: "هل نوافق على القرار؟"
- عداد التصويت: "موافقون: X من Y"
- Progress bar صغير

### الأزرار:
- Primary: 👍 صوّت (لون #C8F169)
- Secondary: 🔄 إعادة (مرة واحدة فقط)

### ملاحظة مهمة:
- **إزالة** كلمة "اعتماد" في هذه المرحلة
- القرار يصبح "معتمد" **فقط بعد** اكتمال الأصوات

---

## 6. حالة الاعتماد (Accepted State)

### التأثيرات:
- أنيميشن ✓ تظهر فوق النرد
- تغيير الهيدر إلى: "✅ تم اعتماد القرار"

### الأزرار:
- CTA رئيسي واحد: "➗ ابدأ تقسيم المصاريف"

---

## 7. تحسينات بصرية

### الألوان:
- تقليل الـ gradients عموماً
- استخدام gradient **فقط أثناء** أنيميشن الرمي
- حالة النتيجة يجب أن تكون هادئة وواثقة

### التباعد:
- زيادة المسافات العمودية بين:
  - النرد ← النتيجة ← التصويت

### النبرة:
- **مرحة** أثناء الرمي
- **جادة** بعد القرار

---

## 8. تحديثات النصوص (عربي)

### استبدال:
- "اعتماد" ← استخدمها فقط بعد اكتمال التصويت

### نصوص جديدة مقترحة:
- "خلّنا نشوف وش يطلع 🎲"
- "النرد يقرر..."
- "النرد قال كلمته"
- "هل نوافق على القرار؟"
- "النرد اختار التالي"

---

## الملفات المتأثرة

| الملف | نوع التغيير |
|-------|-------------|
| `src/components/dice/AnimatedDice.tsx` | إعادة كتابة شاملة - أنيميشن جديدة |
| `src/components/dice/DiceDecision.tsx` | تحديث التدفق والحالات |
| `src/components/dice/DiceResult.tsx` | إعادة تصميم - Layout عمودي للـ Dual |
| `tailwind.config.ts` | إضافة keyframes جديدة |
| `src/i18n/locales/ar/dice.json` | تحديث النصوص |
| `src/i18n/locales/en/dice.json` | تحديث النصوص |

---

## التفاصيل التقنية

### مكون AnimatedDice الجديد:

```tsx
// حالة الرمي - مكعب يهتز
<div className={cn(
  "w-32 h-32 rounded-2xl bg-card border-2 border-primary/30",
  "flex items-center justify-center",
  isRolling && "animate-dice-shake"
)}>
  <Dice5 className="w-16 h-16 text-primary" />
</div>

// حالة الكشف - النرد يتحول للنتيجة
<div className={cn(
  "animate-dice-flip-reveal",
  "w-32 h-32 rounded-2xl bg-card"
)}>
  <span className="text-6xl">{resultFace.emoji}</span>
  <p className="font-bold">{resultFace.label}</p>
</div>
```

### Keyframes جديدة في tailwind.config.ts:

```typescript
'dice-shake': {
  '0%, 100%': { transform: 'rotate(0deg) scale(1)' },
  '10%, 30%, 50%, 70%, 90%': { transform: 'rotate(-8deg) scale(1.02)' },
  '20%, 40%, 60%, 80%': { transform: 'rotate(8deg) scale(1.02)' }
},
'dice-flip-reveal': {
  '0%': { transform: 'rotateY(0deg) scale(1)', opacity: '1' },
  '50%': { transform: 'rotateY(90deg) scale(0.9)', opacity: '0.5' },
  '100%': { transform: 'rotateY(180deg) scale(1)', opacity: '1' }
},
'result-expand': {
  '0%': { transform: 'scale(0.8)', opacity: '0' },
  '100%': { transform: 'scale(1)', opacity: '1' }
}
```

### حالات جديدة في DiceDecision:

```typescript
type DiceState = 
  | 'picker'      // اختيار نوع النرد
  | 'ready'       // جاهز للرمي
  | 'rolling'     // جاري الرمي (أنيميشن)
  | 'revealing'   // كشف النتيجة على النرد ← جديد!
  | 'result'      // عرض بطاقة النتيجة الكاملة
  | 'share';      // المشاركة
```

### ترجمات جديدة:

```json
// ar/dice.json
{
  "dialog": {
    "dice_deciding": "النرد يقرر...",
    "lets_see": "خلّنا نشوف وش يطلع 🎲",
    "dice_said": "النرد قال كلمته",
    "dice_chose": "النرد اختار التالي"
  },
  "voting": {
    "do_we_agree": "هل نوافق على القرار؟",
    "agreed": "موافقون",
    "of": "من"
  }
}
```

---

## معايير النجاح

- ✅ المستخدم يفهم بوضوح **متى** النرد يُرمى
- ✅ المستخدم يرى النرد **وهو يقرر**
- ✅ النتيجة تبدو عشوائية لكن عادلة
- ✅ التصويت يشعر كعمل جماعي، ليس خياراً إجبارياً
- ✅ التجربة الكاملة تبدو مرحة، ليست رسمية

