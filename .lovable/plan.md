

# خطة جعل الريال السعودي أول اختيار في العملات

## الوضع الحالي
- العملات تُحمّل من قاعدة البيانات مرتبة حسب `code` أبجدياً
- الريال السعودي (SAR) يظهر في مكانه الأبجدي (حرف S)
- العملات مُجمّعة حسب المنطقة (الخليج، أوروبا، إلخ)

## الحل المقترح
تعديل مكون `CurrencySelector` لترتيب العملات بحيث يكون الريال السعودي (SAR) أولاً دائماً، ثم باقي العملات مرتبة حسب المنطقة.

---

## التغييرات المطلوبة

### الملف: `src/components/ui/currency-selector.tsx`

**التغيير:** إضافة منطق لترتيب العملات مع أولوية الريال السعودي

```tsx
// إضافة بعد السطر 49 (selectedCurrency)

// Sort currencies with SAR first, then by region
const sortedCurrencies = React.useMemo(() => {
  return [...currencies].sort((a, b) => {
    // SAR always first
    if (a.code === 'SAR') return -1;
    if (b.code === 'SAR') return 1;
    // Then sort by region, then by code
    const regionA = getRegion(a);
    const regionB = getRegion(b);
    if (regionA !== regionB) return regionA.localeCompare(regionB);
    return a.code.localeCompare(b.code);
  });
}, [currencies, getRegion]);

// Group currencies by region (استخدام sortedCurrencies بدلاً من currencies)
const groupedCurrencies = sortedCurrencies.reduce((acc, currency) => {
  // ... نفس المنطق
}, {} as Record<string, Currency[]>);
```

---

## النتيجة المتوقعة

### قبل
```
▼ أوروبا
  🇪🇺 EUR يورو
  🇬🇧 GBP جنيه إسترليني
▼ الخليج
  🇦🇪 AED درهم إماراتي
  🇸🇦 SAR ريال سعودي  ← في مكانه الأبجدي
```

### بعد
```
▼ الخليج
  🇸🇦 SAR ريال سعودي  ← أول اختيار ✓
  🇦🇪 AED درهم إماراتي
  🇧🇭 BHD دينار بحريني
▼ أوروبا
  🇪🇺 EUR يورو
  🇬🇧 GBP جنيه إسترليني
```

---

## الملفات المتأثرة

| الملف | التغيير |
|-------|---------|
| `src/components/ui/currency-selector.tsx` | ترتيب العملات مع أولوية SAR |

