

# خطة إضافة صورة OG مخصصة لصفحة `/launch`

## الهدف
استخدام الصورة المرفقة الجاهزة كصورة OG لصفحة `/launch` لتحسين معاينة واتساب.

---

## التغييرات المطلوبة

### 1. نسخ الصورة إلى المشروع

**المصدر**: `user-uploads://IMG_5758.png`  
**الوجهة**: `public/og/launch-1200x630.png`

سيتم إنشاء مجلد `/public/og/` إذا لم يكن موجوداً.

### 2. تحديث `og-handler` Edge Function

**الملف**: `supabase/functions/og-handler/index.ts`

**التغيير**: استخدام صورة مخصصة لكل مسار بدلاً من صورة واحدة عامة:

```typescript
// تحديث pageMetadata ليشمل الصورة
const pageMetadata: Record<string, { title: string; description: string; image?: string }> = {
  '/launch': {
    title: 'القسمة دايمًا تلخبط؟ خلّها واضحة',
    description: 'تطبيق بسيط يخلي القسمة بين الأصدقاء عادلة بدون إحراج ولا نقاش',
    image: '/og/launch-1200x630.png',  // صورة مخصصة
  },
  '/from': {
    title: 'استخدمت تطبيق بسيط للقسمة بين الأصدقاء',
    description: 'خصوصًا في الشعبنة، ريحنا من اللخبطة. اللي حاب يجرّبه 👇',
    // بدون صورة = يستخدم الافتراضية
  },
};

// في generateOgHtml:
const ogImage = metadata.image 
  ? `${appUrl}${metadata.image}` 
  : `${appUrl}/og-image.png`;
```

### 3. تحديث `LaunchPage.tsx`

**الملف**: `src/pages/LaunchPage.tsx`

**التغيير**: تحديث مسار `ogImage` في SEO component:

```tsx
<SEO 
  title="القسمة دايمًا تلخبط؟ خلّها واضحة"
  description="تطبيق بسيط يخلي القسمة بين الأصدقاء عادلة بدون إحراج ولا نقاش"
  ogImage="https://diviso.app/og/launch-1200x630.png"  // الصورة الجديدة
  noIndex={false}
/>
```

### 4. تحديث `public/launch/index.html`

**الملف**: `public/launch/index.html`

**التغيير**: تحديث وسوم `og:image` و `twitter:image`:

```html
<meta property="og:image" content="https://diviso.app/og/launch-1200x630.png">
<meta property="og:image:width" content="1200">
<meta property="og:image:height" content="630">
...
<meta name="twitter:image" content="https://diviso.app/og/launch-1200x630.png">
```

---

## ملخص الملفات

| الملف | نوع التغيير |
|-------|------------|
| `public/og/launch-1200x630.png` | **جديد** - نسخ الصورة المرفقة |
| `supabase/functions/og-handler/index.ts` | تحديث - دعم صور مخصصة لكل مسار |
| `src/pages/LaunchPage.tsx` | تحديث - مسار ogImage الجديد |
| `public/launch/index.html` | تحديث - وسوم og:image |

---

## النتيجة المتوقعة

عند لصق رابط المشاركة في واتساب:
- **صورة كبيرة**: 1200×630 بنص "القسمة دايمًا تلخبط؟ خلّها واضحة وبلا إحراج"
- **عنوان**: "القسمة دايمًا تلخبط؟ خلّها واضحة"
- **وصف**: "تطبيق بسيط يخلي القسمة بين الأصدقاء عادلة..."

