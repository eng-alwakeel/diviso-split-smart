

# خطة تصغير الشعار للجوال

## المشكلة
الشعار حالياً بحجم ثابت `h-8` (32px) وهو كبير نسبياً على شاشات الجوال، مما يجعل الهيدر يبدو مزدحماً.

## الحل
استخدام أحجام responsive للشعار:
- **الجوال**: `h-6` (24px)
- **الشاشات الأكبر**: `h-8` (32px)

---

## التغيير المطلوب

### الملف: `src/components/AppHeader.tsx`

**السطر 208:**

| قبل | بعد |
|-----|-----|
| `className="h-8 w-auto"` | `className="h-6 sm:h-8 w-auto"` |
| `height={32}` | `height={24}` (للجوال) |

**الكود الجديد:**
```tsx
<img 
  src={appLogo} 
  alt={t('header.logo_alt')} 
  className="h-6 sm:h-8 w-auto" 
  width={128} 
  height={32} 
  style={{ aspectRatio: '128 / 32' }} 
/>
```

---

## الشكل النهائي

### الهيدر على الجوال
```
+------------------------------------------+
| [شارات] [👤]   [diviso صغير]   [546] [🔔] |
+------------------------------------------+
```

### الهيدر على الديسكتوب
```
+--------------------------------------------------+
| [شارات] [👤]      [diviso عادي]      [546] [🔔] |
+--------------------------------------------------+
```

---

## الملفات المتأثرة

| الملف | التغيير |
|-------|---------|
| `src/components/AppHeader.tsx` | تصغير الشعار responsive |

