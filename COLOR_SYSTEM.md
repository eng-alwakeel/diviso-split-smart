# 🎨 نظام الألوان - دليل الاستخدام

## الألوان الأساسية للبراند

- **Primary**: `#C8F169` - اللون الأساسي الأخضر اللموني
- **Background**: `#1A1C1E` - الخلفية الداكنة الرئيسية
- **Foreground**: `#F3F4F6` - لون النص الفاتح

## ألوان الحالات (Status Colors)

### ✅ Success (النجاح)
- **الاستخدام**: عمليات ناجحة، توفير في الميزانية، إنجازات
- **الكلاس**: `bg-success`, `text-success`, `border-success`
- **اللون**: أخضر متناسق مع البراند

### ⚠️ Warning (التحذير)
- **الاستخدام**: تحذيرات، اقتراب من الحد، تنبيهات مهمة
- **الكلاس**: `bg-warning`, `text-warning`, `border-warning`
- **اللون**: برتقالي دافئ متناسق مع الأخضر

### 🚫 Destructive (الخطر)
- **الاستخدام**: أخطاء، تجاوز الحدود، عمليات خطرة
- **الكلاس**: `bg-destructive`, `text-destructive`, `border-destructive`
- **اللون**: أحمر غير قاسي

### ℹ️ Info (المعلومات)
- **الاستخدام**: معلومات عامة، إشعارات محايدة
- **الكلاس**: `bg-info`, `text-info`, `border-info`
- **اللون**: أخضر أفتح من Primary

## ألوان الحالات المالية

### 💰 Status Positive (إيجابي)
- **الاستخدام**: رصيد لك، مدفوع، دخل
- **الكلاس**: `text-status-positive`, `bg-status-positive-bg`
- **اللون**: يستخدم Primary Color

### 💸 Status Negative (سلبي)
- **الاستخدام**: رصيد عليك، مستحق، دين
- **الكلاس**: `text-status-negative`, `bg-status-negative-bg`
- **اللون**: يستخدم Destructive Color

### ⚪ Status Neutral (محايد)
- **الاستخدام**: معلومات محايدة، لا إيجابي ولا سلبي
- **الكلاس**: `text-status-neutral`, `bg-status-neutral-bg`
- **اللون**: رمادي محايد

## ألوان خاصة

### 🔔 Notification Badge
- **الاستخدام**: شارة الإشعارات غير المقروءة
- **الكلاس**: `bg-notification-badge`, `text-notification-badge-foreground`
- **اللون**: يستخدم Primary Color

## ❌ تجنب استخدام ألوان Tailwind المباشرة

لا تستخدم:
- `bg-orange-*`, `text-orange-*`
- `bg-red-*`, `text-red-*`
- `bg-green-*`, `text-green-*`
- `bg-yellow-*`, `text-yellow-*`
- `bg-blue-*`, `text-blue-*`
- `bg-purple-*`, `text-purple-*`

## ✅ استخدم بدلاً منها

| بدلاً من | استخدم |
|---------|--------|
| `bg-orange-500` | `bg-warning` |
| `text-red-600` | `text-destructive` |
| `bg-green-50` | `bg-success/10` أو `bg-status-positive-bg` |
| `text-blue-600` | `text-primary` |
| `bg-purple-50` | `bg-accent/10` |
| `text-yellow-600` | `text-warning` |

## مثال على الاستخدام الصحيح

### Alert Components
```tsx
// ✅ صحيح
<Alert variant="warning">تحذير: اقترب من الحد</Alert>
<Alert variant="destructive">خطأ: تجاوز الحد</Alert>
<Alert variant="success">نجح: تم الحفظ</Alert>

// ❌ خطأ
<Alert className="bg-orange-50 text-orange-600">تحذير</Alert>
```

### Badge Components
```tsx
// ✅ صحيح
<Badge variant="warning">معلق</Badge>
<Badge variant="success">مكتمل</Badge>
<Badge variant="destructive">مرفوض</Badge>

// ❌ خطأ
<Badge className="bg-yellow-100 text-yellow-800">معلق</Badge>
```

### Status Display
```tsx
// ✅ صحيح
<div className="text-status-positive">+500 ر.س</div>
<div className="text-status-negative">-300 ر.س</div>

// ❌ خطأ
<div className="text-green-600">+500 ر.س</div>
<div className="text-red-600">-300 ر.س</div>
```

## كيفية إضافة لون جديد

إذا احتجت لون جديد:

1. **أضف المتغير في `index.css`**:
```css
:root {
  --new-color: [hsl values];
  --new-color-foreground: [hsl values];
}
```

2. **أضف التعريف في `tailwind.config.ts`**:
```typescript
colors: {
  'new-color': {
    DEFAULT: 'hsl(var(--new-color))',
    foreground: 'hsl(var(--new-color-foreground))'
  }
}
```

3. **وثّق الاستخدام في هذا الملف**

## قواعد مهمة

1. **جميع الألوان يجب أن تكون HSL** في `index.css`
2. **استخدم `hsl(var(...))` في `tailwind.config.ts`**
3. **لا تخلط بين RGB و HSL**
4. **استخدم الألوان الدلالية (Semantic) دائماً**
5. **اختبر في Light و Dark Mode**

## التحقق من الألوان

قبل إضافة أي لون جديد، تأكد من:
- [ ] متوافق مع البراند
- [ ] يعمل في Dark Mode
- [ ] التباين كافٍ للقراءة (WCAG AA)
- [ ] له معنى واضح ودلالي
- [ ] موثّق في هذا الملف

## الصيانة

- مراجعة الألوان كل 3 أشهر
- إزالة الألوان غير المستخدمة
- تحديث التوثيق عند أي تغيير
- اختبار التوافق مع الوضع الداكن

---

آخر تحديث: [تاريخ اليوم]
الإصدار: 1.0.0
