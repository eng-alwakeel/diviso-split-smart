
# خطة: تفعيل /launch كرابط مشاركة رسمي مع ترحيب ذكي

## الهدف
تحويل `/launch` لنقطة دخول رئيسية للمشاركة الخارجية مع ترحيب واضح وتجربة سلسة.

---

## الوضع الحالي

| العنصر | الحالة |
|--------|--------|
| صفحة `/launch` تعمل | ✅ موجود |
| 9 سيناريوهات (3 أساسية + 6 ثانوية) | ✅ موجود |
| تجربة تفاعلية (تغيير مبلغ + دافع) | ✅ موجود |
| رسالة ترحيب داخل DemoExperience | ✅ موجود |
| OG Tags ديناميكية حسب ?demo= | ✅ موجود |
| **رسالة ترحيب في صفحة الاختيار (بدون demo)** | ❌ غير موجود |
| **السيناريو العاشر (شلة الجمعة)** | ❌ غير موجود |
| **زر مشاركة واضح في صفحة الاختيار** | ❌ غير موجود |

---

## الملفات المطلوب تعديلها

| الملف | التعديل |
|-------|---------|
| `src/pages/LaunchPage.tsx` | إضافة رسالة ترحيب + زر مشاركة |
| `src/data/demoScenarios.ts` | إضافة سيناريو "شلة الجمعة" |
| `src/lib/share.ts` | إضافة دالة مشاركة الصفحة العامة |
| `supabase/functions/og-handler/index.ts` | إضافة OG للسيناريو الجديد |

---

## 1. تعديل `LaunchPage.tsx`

### A) تحديث العنوان والوصف

**الحالي:**
```text
دايم واحد يدفع أكثر؟
اختر سيناريو وجرب بنفسك
وشوف كيف تنحسب القسمة بدون إحراج
```

**الجديد:**
```text
👋 أهلًا بك في Diviso
قسّم مصاريفك مع الناس اللي معك بدون لخبطة ولا إحراج.
اختر مثال وجرب بنفسك 👇
```

### B) إضافة زر مشاركة واضح

```typescript
{/* Share Button - في الأعلى بعد الوصف */}
<Button
  variant="outline"
  onClick={handleSharePage}
  className="gap-2"
>
  <Link2 className="h-4 w-4" />
  🔗 شارك مع أصحابك
</Button>
```

### C) إضافة دالة المشاركة

```typescript
const handleSharePage = useCallback(async () => {
  const shareUrl = `${window.location.origin}/launch`;
  const shareText = 'قسّم مصاريفك مع أصحابك بدون إحراج – جرب بنفسك';

  if (navigator.share) {
    try {
      await navigator.share({
        title: 'Diviso – القسمة بدون إحراج',
        text: shareText,
        url: shareUrl,
      });
    } catch (error) {
      // fallback to clipboard
    }
  } else {
    await navigator.clipboard.writeText(`${shareText}\n${shareUrl}`);
    toast({ title: 'تم نسخ الرابط!' });
  }
  
  trackEvent('launch_page_shared');
}, [trackEvent]);
```

### D) تحديث VALID_SCENARIOS

```typescript
const VALID_SCENARIOS: ScenarioType[] = [
  'travel', 'friends', 'housing',
  'activities', 'desert', 'groups', 'family', 'carpool', 'events', 'friday'  // ← إضافة friday
];
```

---

## 2. تعديل `demoScenarios.ts`

### A) تحديث ScenarioType

```typescript
export type ScenarioType = 
  | 'travel' | 'friends' | 'housing'  // Primary
  | 'activities' | 'desert' | 'groups' | 'family' | 'carpool' | 'events' | 'friday';  // Secondary
```

### B) إضافة سيناريو "شلة الجمعة"

```typescript
{
  id: 'friday',
  tier: 'secondary',
  icon: '👬',
  title: 'شلة الجمعة',
  subtitle: 'طلعات – كشتات – قهوة',
  groupName: 'شلة الجمعة',
  currency: 'ر.س',
  shareText: 'شلة الجمعة؟ Diviso يخلي القسمة بينكم عادلة بدون نقاش.',
  members: [
    { id: 'm1', name: 'عبدالعزيز', avatar: 'ع' },
    { id: 'm2', name: 'فارس', avatar: 'ف' },
    { id: 'm3', name: 'بندر', avatar: 'ب' },
    { id: 'm4', name: 'ثامر', avatar: 'ث' },
  ],
  expenses: [
    { id: 'e1', description: 'مطعم', amount: 320, paidById: 'm1', icon: '🍽️' },
    { id: 'e2', description: 'قهوة', amount: 100, paidById: 'm2', icon: '☕' },
    { id: 'e3', description: 'آيسكريم', amount: 60, paidById: 'm3', icon: '🍦' },
  ],
}
```

---

## 3. تعديل `share.ts`

### A) إضافة دالة مشاركة الصفحة العامة

```typescript
export async function shareLaunchPage(): Promise<ShareResult> {
  const shareUrl = `${window.location.origin}/launch`;
  const shareText = 'قسّم مصاريفك مع أصحابك بدون إحراج – جرب بنفسك';

  if (navigator.share) {
    try {
      await navigator.share({
        title: 'Diviso – القسمة بدون إحراج',
        text: shareText,
        url: shareUrl,
      });
      return { success: true, method: 'native' };
    } catch (error) {
      if ((error as Error).name === 'AbortError') {
        return { success: false, method: 'native', error: 'cancelled' };
      }
    }
  }

  try {
    await navigator.clipboard.writeText(`${shareText}\n${shareUrl}`);
    return { success: true, method: 'clipboard' };
  } catch (error) {
    return { 
      success: false, 
      method: 'clipboard', 
      error: (error as Error).message 
    };
  }
}
```

---

## 4. تعديل `og-handler/index.ts`

### A) إضافة السيناريو الجديد

```typescript
friday: {
  title: 'شلة الجمعة؟',
  description: 'Diviso يخلي القسمة بينكم عادلة بدون نقاش.',
},
```

---

## شكل الـ UI النهائي

```text
┌─────────────────────────────────────────────────┐
│                    [Logo]                       │
│                                                 │
│           👋 أهلًا بك في Diviso                 │
│                                                 │
│      قسّم مصاريفك مع الناس اللي معك             │
│         بدون لخبطة ولا إحراج.                   │
│        اختر مثال وجرب بنفسك 👇                  │
│                                                 │
│        [🔗 شارك مع أصحابك]                      │
│                                                 │
├─────────────────────────────────────────────────┤
│  ┌─────────┐  ┌─────────┐  ┌─────────┐         │
│  │   ✈️    │  │  🧑‍🤝‍🧑  │  │   🏠    │         │
│  │  سفر   │  │  طلعة  │  │  سكن   │         │
│  │[جرّب]  │  │[جرّب]  │  │[جرّب]  │         │
│  └─────────┘  └─────────┘  └─────────┘         │
│                                                 │
│         [ عرض المزيد من التجارب ↓ ]            │
│                                                 │
├─────────────────────────────────────────────────┤
│              تجارب إضافية                       │
│  ┌───────┐  ┌───────┐  ┌───────┐               │
│  │  🎯   │  │  🏕️   │  │  👥   │               │
│  │ نشاط │  │رحلة بر│  │مجموعة│               │
│  └───────┘  └───────┘  └───────┘               │
│  ┌───────┐  ┌───────┐  ┌───────┐  ┌───────┐   │
│  │ 👨‍👩‍👧  │  │  🚗   │  │  🎉   │  │  👬   │   │
│  │ عائلة│  │مشوار │  │مناسبة│  │شلة   │   │
│  └───────┘  └───────┘  └───────┘  │الجمعة│   │
│                                    └───────┘   │
└─────────────────────────────────────────────────┘
```

---

## الروابط النهائية (11 رابط)

| # | الرابط | الوصف |
|---|--------|-------|
| 1 | `/launch` | صفحة الاختيار العامة |
| 2 | `/launch?demo=travel` | سفر ✈️ |
| 3 | `/launch?demo=friends` | طلعة أصدقاء 🧑‍🤝‍🧑 |
| 4 | `/launch?demo=housing` | سكن مشترك 🏠 |
| 5 | `/launch?demo=activities` | نشاط 🎯 |
| 6 | `/launch?demo=desert` | رحلة بر 🏕️ |
| 7 | `/launch?demo=groups` | مجموعة 👥 |
| 8 | `/launch?demo=family` | عائلة 👨‍👩‍👧 |
| 9 | `/launch?demo=carpool` | مشوار مشترك 🚗 |
| 10 | `/launch?demo=events` | مناسبة 🎉 |
| 11 | `/launch?demo=friday` | شلة الجمعة 👬 |

---

## OG Tags

### بدون ?demo=

```
Title: 👋 أهلًا بك في Diviso
Description: قسّم مصاريفك مع الناس اللي معك بدون لخبطة ولا إحراج.
```

### مع ?demo=carpool

```
Title: مشوار مشترك؟ خلّ القسمة واضحة 🚗
Description: جرّب مثال سريع وشوف كم لك وكم عليك بدون نقاش.
```

---

## معايير القبول

| # | المعيار |
|---|---------|
| 1 | رسالة ترحيب واضحة في صفحة الاختيار ✔ |
| 2 | زر مشاركة واضح يعمل (Native + Clipboard) ✔ |
| 3 | 10 سيناريوهات متوفرة ✔ |
| 4 | OG Tags ديناميكية تعمل ✔ |
| 5 | لا تغيير على الصفحة الرئيسية / ✔ |
| 6 | كل التجارب تفاعلية ✔ |

---

## ملخص التغييرات

| الملف | الأسطر | التعقيد |
|-------|--------|---------|
| `LaunchPage.tsx` | ~25 سطر تعديل | بسيط |
| `demoScenarios.ts` | ~25 سطر إضافة | بسيط |
| `share.ts` | ~20 سطر إضافة | بسيط |
| `og-handler/index.ts` | ~5 سطر إضافة | بسيط |

**الوقت المتوقع للتنفيذ:** 10-15 دقيقة
