
# خطة إنشاء صفحة الإنفلونسر `/from`

## ملخص المتطلبات

صفحة مخصصة لحملات الإنفلونسرز تُستخدم في Stories و Bio و Posts. الهدف هو نقل تجربة شخصية صادقة بدون إحساس تسويقي، مع تحويل المتابعين إلى مستخدمين مسجلين.

---

## الخصائص المطلوبة

| البند | التفصيل |
|-------|---------|
| **الرابط** | `/from` (مع UTM parameters للتتبع) |
| **الصيغة** | ضمير المتكلم (كأن الإنفلونسر يتكلم) |
| **التصميم** | مشابه لـ `/launch` - بسيط، بدون تشتيت |
| **CTA** | زر واحد فقط "جرّبه الحين" |
| **السلوك** | غير مسجّل → تسجيل، مسجّل → إنشاء مجموعة |

---

## المحتوى الحرفي

```text
H1 (العنوان):
أنا استخدمت هذا التطبيق وريحني من لخبطة القسمة

الوصف (2-3 أسطر):
في الطلعات والسفر دايم القسمة تلخبط
خصوصًا وقت الشعبنة
هذا التطبيق خلّى كل شي واضح بدون إحراج

الزر:
جرّبه الحين

سطر الثقة:
دقيقة وتبدأ تستخدمه مع مجموعتك
```

---

## Open Graph (للمشاركة)

| الحقل | المحتوى |
|-------|---------|
| **OG:title** | تطبيق خلّى القسمة أوضح بيننا |
| **OG:description** | استخدمته في الطلعات وراح يريحك من لخبطة الحساب |
| **OG:image** | صورة بسيطة مع نص "القسمة... بدون إحراج" وألوان البراند |

---

## التتبع (Tracking Events)

سيتم تتبع الأحداث التالية عبر GTM/GA4 مع دعم UTM الكامل:

1. `influencer_page_view` - عند فتح الصفحة
2. `influencer_cta_click` - عند الضغط على الزر

**UTM Convention:**
```text
diviso.app/from?utm_source=infl_name&utm_medium=influencer&utm_campaign=sha3bana
```

---

## ملف الصفحة الجديد

سيتم إنشاء الملف: `src/pages/InfluencerPage.tsx`

### هيكل الكود:

```text
InfluencerPage.tsx
├── SEO Component (Custom OG metadata)
├── Logo (BRAND_CONFIG.logo)
├── H1 Title (ضمير المتكلم)
├── Description (2-3 lines)
├── CTA Button (جرّبه الحين)
└── Trust Line (دقيقة وتبدأ...)
```

### السلوك:
- عند الضغط على الزر:
  - التحقق من الجلسة (session)
  - إذا مسجّل → `/create-group`
  - إذا غير مسجّل → `/auth?mode=signup&redirect=/create-group`

---

## التعديلات المطلوبة

### 1. إنشاء ملف جديد
**الملف:** `src/pages/InfluencerPage.tsx`

يحتوي على:
- مكون SEO مع metadata خاصة للإنفلونسر
- تصميم بسيط مشابه لـ LaunchPage
- تتبع أحداث influencer_page_view و influencer_cta_click
- دعم UTM parameters

### 2. تحديث App.tsx
إضافة Route جديد:
```text
<Route path="/from" element={<LazyInfluencerPage />} />
```

### 3. إنشاء صورة OG مخصصة
**الملف:** `public/og-influencer.png`

صورة بسيطة 1200x630 تحتوي:
- خلفية بألوان البراند (Dark + Lime Green)
- النص: "القسمة... بدون إحراج"
- شعار Diviso

---

## نص الإنفلونسر للمشاركة

```text
استخدمت تطبيق بسيط للقسمة بين الأصدقاء
خصوصًا في الشعبنة
ريحنا من اللخبطة
اللي حاب يجرّبه 👇

diviso.app/from?utm_source=INFL_NAME&utm_medium=influencer&utm_campaign=sha3bana
```

---

## الممنوعات المُطبقة

| ممنوع | مُطبق |
|-------|-------|
| خصومات | لا يوجد |
| أكواد | لا يوجد |
| أسعار | لا يوجد |
| اشتراكات | لا يُذكر |
| "حمّل الآن" | لا يُستخدم |
| Popups | لا يوجد |

---

## تفاصيل تقنية

### الملفات المتأثرة:

| الملف | التغيير |
|-------|---------|
| `src/pages/InfluencerPage.tsx` | **جديد** - صفحة الإنفلونسر |
| `src/App.tsx` | إضافة Route `/from` |
| `public/og-influencer.png` | **جديد** - صورة OG للمشاركة |

### الاعتمادات:

- `@/components/ui/button`
- `@/lib/brandConfig` (BRAND_CONFIG)
- `@/integrations/supabase/client`
- `@/hooks/useGoogleAnalytics` (trackWithUTM)
- `@/components/SEO`
- `react-router-dom` (useNavigate)

### Pattern المُتبع:

سيتم اتباع نفس pattern صفحة `/launch` (LaunchPage.tsx) لضمان التناسق:
- RTL direction
- Centered layout
- Minimal design
- Brand colors
- UTM tracking

