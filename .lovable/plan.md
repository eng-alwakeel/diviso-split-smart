
# إصلاح عرض نوع الاشتراك في SubscriptionCard

## المشكلة
من الصورة، بطاقة "الخطة الحالية" تعرض:
- Badge علوي: `🆓 مجاني` ✅
- قسم "الحالة": يعرض `مجاني` ❌

المشكلة أن "الحالة" يجب أن تعرض (نشط/تجريبي/منتهي/غير مشترك) وليس نوع الخطة (مجاني/Starter/Pro/Max).

---

## الحل

### تغيير منطق `getStatusText()` في SubscriptionCard.tsx

**قبل:**
```typescript
const getStatusText = () => {
  if (!subscription) return isRTL ? "مجاني" : "Free"; // ❌ هذا نوع خطة وليس حالة
  ...
};
```

**بعد:**
```typescript
const getStatusText = () => {
  if (!subscription) return isRTL ? "غير مشترك" : "Not Subscribed"; // ✅ هذه حالة
  ...
};
```

---

## تحسين إضافي: تبسيط عرض المستخدم المجاني

للمستخدم المجاني، البطاقة تبدو فارغة (عمود واحد فقط). 

### الحل: إخفاء قسم "الحالة" للمستخدم المجاني وعرض رسالة تشجيعية بدلاً منه

**التصميم الجديد للمستخدم المجاني:**

```text
┌────────────────────────────────────────────┐
│  👑 الخطة الحالية              [🆓 مجاني]  │
│  إدارة اشتراكك وخطتك                       │
│                                            │
│  ┌────────────────────────────────────┐   │
│  │  ⚡ ترقَّ للحصول على مميزات أكثر    │   │
│  │  نقاط إضافية • تصنيف ذكي • أولوية  │   │
│  └────────────────────────────────────┘   │
│                                            │
│  [ اشترك الآن 👑 ]                         │
└────────────────────────────────────────────┘
```

**التصميم للمستخدم المشترك:**

```text
┌────────────────────────────────────────────┐
│  👑 الخطة الحالية              [💎 Pro]    │
│  إدارة اشتراكك وخطتك                       │
│                                            │
│  ┌──────────┐  ┌──────────┐               │
│  │ الحالة   │  │ متبقي    │               │
│  │ ✓ نشط   │  │ 25 يوم   │               │
│  └──────────┘  └──────────┘               │
│                                            │
│  ████████████░░░░░░  60% من الفترة        │
│                                            │
│  [ إدارة الاشتراك ]                        │
└────────────────────────────────────────────┘
```

---

## التغييرات التقنية

### الملف: `src/components/settings/SubscriptionCard.tsx`

| التغيير | التفاصيل |
|---------|----------|
| تعديل `getStatusText()` | تغيير "مجاني" إلى "غير مشترك" |
| إضافة شرط للعرض المختلف | إذا `isFreePlan` → عرض رسالة تشجيعية بدل grid الحالة |
| تحسين UX | إضافة قسم يشرح مميزات الترقية للمستخدم المجاني |

---

## الكود المعدل

```typescript
// SubscriptionCard.tsx - التعديلات

// 1. تعديل getStatusText
const getStatusText = () => {
  if (!subscription) return isRTL ? "غير مشترك" : "Not Subscribed";
  // ... باقي الكود كما هو
};

// 2. في CardContent، إضافة شرط للعرض المختلف:
{isFreePlan ? (
  // عرض مبسط للمستخدم المجاني مع رسالة تشجيعية
  <div className="p-4 rounded-lg bg-gradient-to-r from-primary/10 to-primary/5 border border-primary/20">
    <div className="flex items-center gap-2 mb-2">
      <Sparkles className="w-4 h-4 text-primary" />
      <span className="font-medium text-sm">
        {isRTL ? "ترقَّ للحصول على المزيد" : "Upgrade for more"}
      </span>
    </div>
    <p className="text-xs text-muted-foreground">
      {isRTL 
        ? "نقاط إضافية • تصنيف ذكي • أولوية الدعم"
        : "Extra points • Smart classification • Priority support"
      }
    </p>
  </div>
) : (
  // العرض الحالي للمستخدمين المشتركين (grid + progress)
  <>
    {/* Status grid */}
    {/* Progress bar */}
  </>
)}
```

---

## ملخص التغييرات

| الإجراء | الملف |
|---------|-------|
| تعديل نص الحالة | `src/components/settings/SubscriptionCard.tsx` |
| تحسين عرض المستخدم المجاني | نفس الملف |
| إضافة أيقونة Sparkles | import جديد من lucide-react |

---

## النتيجة

- ✅ نص "الحالة" صحيح: "غير مشترك" بدلاً من "مجاني"
- ✅ تجربة أفضل للمستخدم المجاني مع رسالة تشجيعية
- ✅ البطاقة لا تبدو فارغة للمستخدمين المجانيين
