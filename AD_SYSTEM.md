# 📢 نظام الإعلانات الموحد - دليل الاستخدام

## 🎯 نظرة عامة

تم تصميم نظام الإعلانات الموحد لتوفير تجربة إعلانية متسقة عبر جميع صفحات التطبيق للمستخدمين المجانيين. يستخدم النظام `UnifiedAdLayout` كمكون أساسي لإدارة عرض الإعلانات.

---

## 🏗️ البنية الأساسية

### المكون الرئيسي: `UnifiedAdLayout`

```tsx
import { UnifiedAdLayout } from '@/components/ads/UnifiedAdLayout';

<UnifiedAdLayout 
  placement="page_name"
  showTopBanner={true}
  showSidebar={true}
  showBottomBanner={true}
>
  {/* محتوى الصفحة */}
</UnifiedAdLayout>
```

### Parameters

| Parameter | Type | Default | الوصف |
|-----------|------|---------|-------|
| `placement` | string | مطلوب | معرف الصفحة للتتبع (مثال: "dashboard", "expenses") |
| `showTopBanner` | boolean | true | عرض البانر العلوي الثابت |
| `showSidebar` | boolean | true | عرض الشريط الجانبي (xl+ فقط) |
| `showBottomBanner` | boolean | false | عرض البانر السفلي (موبايل فقط) |
| `className` | string | "" | CSS classes إضافية |

---

## 📍 مواضع الإعلانات

### 1. Top Banner (البانر العلوي)
- **الموضع**: أعلى محتوى الصفحة
- **الحالة**: Sticky (ثابت عند التمرير)
- **العرض**: جميع أحجام الشاشات
- **الاستخدام المثالي**: Dashboard, MyExpenses, MyGroups

```tsx
showTopBanner={true}
```

### 2. Sidebar (الشريط الجانبي)
- **الموضع**: الجانب الأيسر
- **الحالة**: Sticky (ثابت عند التمرير)
- **العرض**: xl وأكبر (1280px+)
- **الاستخدام المثالي**: صفحات بمحتوى طويل

```tsx
showSidebar={true}
```

### 3. Bottom Banner (البانر السفلي)
- **الموضع**: أسفل المحتوى
- **الحالة**: Sticky (ثابت أعلى Bottom Navigation)
- **العرض**: الموبايل والتابلت فقط (< lg)
- **الاستخدام المثالي**: Settings, Forms

```tsx
showBottomBanner={true}
```

---

## 📱 استراتيجية العرض حسب الجهاز

### Desktop (xl+)
- ✅ Top Banner
- ✅ Sidebar
- ❌ Bottom Banner

### Tablet (md - lg)
- ✅ Top Banner
- ❌ Sidebar
- ✅ Bottom Banner

### Mobile (< md)
- ✅ Top Banner
- ❌ Sidebar
- ✅ Bottom Banner

---

## 🎨 أمثلة الاستخدام

### Dashboard (كامل الإعلانات)
```tsx
<UnifiedAdLayout 
  placement="dashboard"
  showTopBanner={true}
  showSidebar={true}
  showBottomBanner={true}
>
  <div className="page-container">
    {/* Dashboard content */}
  </div>
</UnifiedAdLayout>
```

### Settings (إعلانات خفيفة)
```tsx
<UnifiedAdLayout 
  placement="settings"
  showTopBanner={false}
  showSidebar={false}
  showBottomBanner={true}
>
  <div className="page-container">
    {/* Settings content */}
  </div>
</UnifiedAdLayout>
```

### Form Pages (إعلان سفلي فقط)
```tsx
<UnifiedAdLayout 
  placement="add_expense"
  showTopBanner={false}
  showSidebar={false}
  showBottomBanner={true}
>
  <div className="form-container">
    {/* Form content */}
  </div>
</UnifiedAdLayout>
```

---

## 🎯 إرشادات التصميم

### متى تستخدم كل موضع

#### Top Banner
✅ **استخدمه في:**
- صفحات Dashboard
- قوائم البيانات (Expenses, Groups)
- صفحات العرض الرئيسية

❌ **تجنبه في:**
- نماذج الإدخال المعقدة
- صفحات المصادقة
- صفحات الدفع

#### Sidebar
✅ **استخدمه في:**
- صفحات بمحتوى طويل وقابل للتمرير
- Dashboard بأقسام متعددة
- صفحات التقارير

❌ **تجنبه في:**
- صفحات ضيقة المحتوى
- نماذج قصيرة
- صفحات ذات تخطيط معقد

#### Bottom Banner
✅ **استخدمه في:**
- صفحات الإعدادات
- نماذج الإدخال
- صفحات النتائج

❌ **تجنبه في:**
- صفحات بها Bottom Navigation مزدحم
- نماذج متعددة الخطوات

---

## 🚀 إضافة الإعلانات لصفحة جديدة

### الخطوات:

1. **Import المكون**
```tsx
import { UnifiedAdLayout } from '@/components/ads/UnifiedAdLayout';
```

2. **Wrap محتوى الصفحة**
```tsx
<UnifiedAdLayout placement="page_name" ...props>
  {/* existing content */}
</UnifiedAdLayout>
```

3. **اختيار مواضع الإعلانات المناسبة**
```tsx
// بناءً على نوع الصفحة:
// - Dashboard-like: showTopBanner + showSidebar + showBottomBanner
// - List-like: showTopBanner + showSidebar
// - Form-like: showBottomBanner only
// - Settings-like: showBottomBanner only
```

4. **اختبار على جميع الأجهزة**
- ✅ Desktop (xl+)
- ✅ Tablet (md-lg)
- ✅ Mobile (< md)

---

## 🔒 منطق عرض الإعلانات

الإعلانات **لا تظهر** للمستخدمين في الحالات التالية:

```tsx
const shouldShowAds = !subscription || 
  subscription.status !== 'active' || 
  !subscription.plan;
```

- ✅ مستخدم مجاني → إعلانات
- ✅ تجربة منتهية → إعلانات
- ❌ Personal Plan (Active) → بدون إعلانات
- ❌ Family Plan (Active) → بدون إعلانات
- ❌ Lifetime Plan → بدون إعلانات

---

## 📊 تتبع الإعلانات

يتم تتبع الإعلانات تلقائياً بناءً على `placement`:

```tsx
placement="dashboard" → Tracks as:
- dashboard_top
- dashboard_sidebar (desktop)
- dashboard_bottom (mobile)
```

---

## ⚠️ أفضل الممارسات

### ✅ افعل

- استخدم `placement` وصفي ومميز لكل صفحة
- اختبر على جميع أحجام الشاشات
- تأكد من عدم تداخل الإعلانات مع المحتوى الرئيسي
- استخدم `showBottomBanner` للموبايل في الصفحات الطويلة

### ❌ لا تفعل

- لا تضع أكثر من 3 مواضع للإعلانات في صفحة واحدة
- لا تستخدم إعلانات في صفحات المصادقة أو الدفع
- لا تجعل الإعلانات تغطي محتوى مهم
- لا تنسى اختبار الأداء على الأجهزة البطيئة

---

## 🛠️ استكشاف الأخطاء

### الإعلانات لا تظهر؟
1. تحقق من حالة الاشتراك
2. تأكد من `placement` صحيح
3. تحقق من حجم الشاشة للـ Sidebar

### الإعلانات تغطي المحتوى؟
1. استخدم `className` لضبط المسافات
2. قلل عدد مواضع الإعلانات
3. اضبط `z-index` إذا لزم الأمر

### الأداء بطيء؟
1. استخدم Lazy Loading
2. قلل عدد الإعلانات المتزامنة
3. تحقق من حجم الصور في الإعلانات

---

## 📝 Checklist للصفحات الجديدة

عند إضافة صفحة جديدة، تأكد من:

- [ ] Import `UnifiedAdLayout`
- [ ] Wrap المحتوى بـ `UnifiedAdLayout`
- [ ] اختيار `placement` مناسب وفريد
- [ ] تحديد مواضع الإعلانات المناسبة
- [ ] اختبار على Desktop
- [ ] اختبار على Tablet
- [ ] اختبار على Mobile
- [ ] التحقق من عدم تداخل الإعلانات
- [ ] اختبار تجربة المستخدم المجاني
- [ ] اختبار تجربة المستخدم المدفوع

---

## 🔄 التحديثات المستقبلية

### في الخطة:
- [ ] Ad Caching System
- [ ] A/B Testing للمواضع
- [ ] Advanced Analytics
- [ ] Personalized Ad Targeting
- [ ] Video Ads Support
- [ ] Native Ads Integration

---

## 📞 الدعم

للمزيد من المساعدة أو الأسئلة:
- راجع `COLOR_SYSTEM.md` للألوان والتصميم
- راجع `BEFORE_COMMIT_CHECKLIST.md` قبل الـ commit
- استشر فريق التطوير للحالات المعقدة
