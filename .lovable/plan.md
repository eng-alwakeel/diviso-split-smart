

# خطة: نقل مفاتيح التنقّل إلى أسفل صفحة /launch

## الوضع الحالي

| العنصر | المكان الحالي | المشكلة |
|--------|---------------|---------|
| زر "شارك مع أصحابك" | أعلى الصفحة (سطر 229-237) | يشتت المستخدم قبل التجربة |
| `StickySignupBar` | Fixed bar أسفل الشاشة | يظهر فقط بعد إكمال التجربة |
| رابط المميزات | غير موجود | ❌ |

## التغييرات المطلوبة

### 1. إزالة زر المشاركة من الـ Hero

```text
قبل:
┌─────────────────────────────────────────┐
│ 👋 أهلًا بك في Diviso                   │
│ ...وصف...                               │
│                                          │
│ [🔗 شارك مع أصحابك]  ← يُحذف من هنا     │
│                                          │
│ [Grid التجارب]                          │
└─────────────────────────────────────────┘
```

### 2. إضافة قسم الأزرار أسفل الصفحة

```text
بعد:
┌─────────────────────────────────────────┐
│ 👋 أهلًا بك في Diviso                   │
│ ...وصف...                               │
│                                          │
│ اختر مثال وجرب بنفسك 👇                  │
│                                          │
│ [Grid التجارب الأساسية]                 │
│                                          │
│ [عرض المزيد من التجارب]                 │
│                                          │
│ [Grid التجارب الإضافية] (إن وجد)         │
│                                          │
│ ─────────────────────────────────────── │
│                                          │
│   خلصت التجربة؟                         │
│   تقدر تسجّل، تشوف المميزات،            │
│   أو تشارك الصفحة مع شلتك               │
│                                          │
│ [سجّل حساب جديد] [شوف المميزات] [🔗]    │
│                                          │
└─────────────────────────────────────────┘
```

---

## التغييرات في الكود

### 1. إزالة زر المشاركة من الـ Hero (سطر 229-237)

```typescript
// يُحذف:
<Button
  variant="outline"
  onClick={handleSharePage}
  className="gap-2 mb-10"
>
  <Link2 className="h-4 w-4" />
  🔗 شارك مع أصحابك
</Button>
```

### 2. تحديث handleSharePage لنص المشاركة الجديد

```typescript
const handleSharePage = useCallback(async () => {
  const shareUrl = `${window.location.origin}/launch`;
  const shareText = 'دايم القسمة تلخبط؟ خلّها واضحة 👇\nجرّب Diviso بدون تسجيل:';

  if (navigator.share) {
    try {
      await navigator.share({
        title: 'Diviso – القسمة بدون إحراج',
        text: shareText,
        url: shareUrl,
      });
      trackEvent('launch_share_clicked', { method: 'native' });
      return;
    } catch (error) {
      if ((error as Error).name === 'AbortError') {
        return;
      }
    }
  }
  
  try {
    await navigator.clipboard.writeText(`${shareText}\n${shareUrl}`);
    toast({ title: 'تم نسخ الرابط!' });
    trackEvent('launch_share_clicked', { method: 'clipboard' });
  } catch (error) {
    console.error('Failed to copy:', error);
  }
}, [trackEvent, toast]);
```

### 3. إضافة Handler للتسجيل مع redirect إلى /launch

```typescript
const handleSignupClick = useCallback(() => {
  trackEvent('launch_signup_clicked');
  navigate('/auth?mode=signup&redirect=/launch');
}, [navigate, trackEvent]);
```

### 4. إضافة Handler للمميزات

```typescript
const handleFeaturesClick = useCallback(() => {
  trackEvent('launch_features_clicked');
  navigate('/');
}, [navigate, trackEvent]);
```

### 5. تحديث handleSharePage لتتبع analytics

```typescript
// تغيير اسم الـ event من 'launch_page_shared' إلى 'launch_share_clicked'
trackEvent('launch_share_clicked', { method: 'native' | 'clipboard' });
```

### 6. إضافة قسم الأزرار أسفل الـ Grid (بعد Secondary Experiences)

```typescript
{/* Bottom Actions Section */}
<section className="w-full max-w-md mt-16 text-center space-y-6">
  {/* Guidance Text */}
  <p className="text-sm text-muted-foreground leading-relaxed">
    خلصت التجربة؟<br />
    تقدر تسجّل، تشوف المميزات، أو تشارك الصفحة مع شلتك
  </p>
  
  {/* Action Buttons */}
  <div className="flex flex-col sm:flex-row gap-3 justify-center items-center">
    {/* Primary: Signup */}
    <Button onClick={handleSignupClick}>
      سجّل حساب جديد
    </Button>
    
    {/* Secondary: Features */}
    <Button variant="outline" onClick={handleFeaturesClick}>
      شوف مميزات التطبيق
    </Button>
    
    {/* Ghost: Share */}
    <Button 
      variant="ghost" 
      size="icon" 
      onClick={handleSharePage}
      aria-label="مشاركة"
    >
      <Link2 className="h-4 w-4" />
    </Button>
  </div>
</section>
```

---

## الملفات المطلوب تعديلها

| الملف | التعديل | الأسطر |
|-------|---------|--------|
| `src/pages/LaunchPage.tsx` | إزالة زر المشاركة من Hero + إضافة قسم الأزرار أسفل | 229-237, 279-280 |

---

## Analytics Events

| Event | متى | Parameters |
|-------|-----|------------|
| `launch_signup_clicked` | عند الضغط على "سجّل حساب جديد" | - |
| `launch_features_clicked` | عند الضغط على "شوف مميزات التطبيق" | - |
| `launch_share_clicked` | عند الضغط على زر المشاركة | `{ method: 'native' \| 'clipboard' }` |

---

## ترتيب الأزرار (RTL)

```text
Desktop (flex-row):
┌────────────────────────────────────────────────────────┐
│ [سجّل حساب جديد]  [شوف مميزات التطبيق]  [🔗]         │
└────────────────────────────────────────────────────────┘

Mobile (flex-col):
┌────────────────────┐
│ [سجّل حساب جديد]   │
│ [شوف مميزات التطبيق]│
│       [🔗]         │
└────────────────────┘
```

---

## معايير القبول

| # | المعيار |
|---|---------|
| 1 | المفاتيح تظهر أسفل الصفحة بعد Grid التجارب |
| 2 | لا يوجد زر مشاركة في الـ Hero |
| 3 | زر التسجيل يوجه إلى `/auth?mode=signup&redirect=/launch` |
| 4 | زر المميزات يوجه إلى `/` |
| 5 | زر المشاركة يعمل بـ Web Share API على الجوال |
| 6 | زر المشاركة يعمل بـ Clipboard على الديسكتوب |
| 7 | نص المشاركة الجديد يُستخدم |
| 8 | التصميم متوافق مع RTL والموبايل |
| 9 | Analytics events تُرسل بشكل صحيح |

---

## ملخص التغييرات

| العنصر | العدد |
|--------|-------|
| ملفات معدلة | 1 |
| عناصر محذوفة | 1 (زر المشاركة من Hero) |
| عناصر مضافة | 1 (قسم الأزرار أسفل الصفحة) |
| Handlers جديدة | 2 (`handleSignupClick`, `handleFeaturesClick`) |
| Analytics events جديدة | 3 |

**الوقت المتوقع للتنفيذ:** 5-10 دقيقة

